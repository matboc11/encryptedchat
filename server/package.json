{
    "name": "encryptedchat-server",
    "description": "",
    "version": "0.0.1",
    "scripts": {
        "start": "ts-node server.ts",
        "keygen": "openssl genrsa -des3 -out private.pem 2048 && openssl rsa -in private.pem -outform PEM -pubout -out public.pem"
    },
    "dependencies": {
        "@types/node": "^13.9.2",
        "esm": "^3.2.25",
        "socket.io": "^2.3.0",
        "ts-node": "^8.7.0",
        "typescript": "^3.8.3"
    }
}
